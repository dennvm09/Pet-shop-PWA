import Vue from 'vue';
import Vuetify from 'vuetify/lib/framework';

Vue.use(Vuetify); /** le estoy diciendo a Vue que use vuetify; es como si le estoy dando una dependencia, pa que la tenga en cuenta */

export default new Vuetify({
});
